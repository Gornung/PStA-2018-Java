package PStA;

/*

Klammersalat

Überprüfen Sie für jede Zeile der Eingabe, ob die Anzahl der Klammern korrekt ist, d.h. dass alle öffnenden Klammern auch geschlossen werden, und geben Sie entsprechend OK aus wenn alles in Ordnung ist. Stimmt die Klammerung nicht, sollen Sie ausgeben - an welcher Stelle (von 0 gezählt) der erste Fehler auftritt, - welches Klammer gefunden wurde (oder - am Ende der Zeile), - und welches Klammer stattdessen erwartet wäre (bzw. - wenn keine).

Beachten Sie: In dieser Version der Aufgabe sind alle ASCII Klammersymbole, also (), [], {} und <>, erlaubt. Achten Sie insbesondere darauf, dass die jeweilige schließende Klammer vom gleichen Symbol sein muss, d.h. Klammerpaare können sich nicht überlappen (siehe Beispiele unten), sondern nur eingebettet sein.

Beispiele:

Eingabe

(Hallo, Welt)
(das ist falsch}
(das { ist auch ) falsch)
das (ist { ein < langes ( aber ) richtiges > [ Beispiel ] } ! )
)
{
(a(b(c)
hans)
Ausgabe

OK
15 } )
16 ) }
OK
0 ) -
1 - }
7 - )
4 ) -

 */

public class Problem23 {
}
